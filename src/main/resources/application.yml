servers:
  mq:
    host: 127.0.0.1
    port: 1425
    queue-manager: TestNodeQM
    channel: ONE.SVR.CONN
    request-queue: ONE.QUEUE
    reply-queue: ONE.QUEUE

spring:
  datasource:
    url: jdbc:oracle:thin:@localhost:1521:xe
    username: test
    password: test
    driver-class-name: oracle.jdbc.driver.OracleDriver

  jpa:
    hibernate:
      ddl-auto: create